<div id="<%= dom_id post %>" class="bg-secondary"> 
  <h2>
    <%= post.title %>
  </h2>

  <h4>
    <%= "Posted by: #{post.user.email}" %>
  </h4>

  <p>
    <%= post.body %>
  </p>

  <p>
    <%= pluralize(post.views, "view") %>
  </p>
</div>
